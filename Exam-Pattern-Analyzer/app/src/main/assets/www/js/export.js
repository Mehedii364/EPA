// EPA Reports and Export Generator (PDF, DOCX, CSV)
const EPAExport = {
  // Export Questions to CSV
  exportCSV(questions, filename = 'exam_pattern_analyzer_questions.csv') {
    if (!questions || questions.length === 0) {
      alert('No question data available to export.');
      return;
    }

    const headers = ['ID', 'Number', 'Question Text', 'Subject', 'Exam Year', 'Section', 'Marks', 'Chapter', 'Topic', 'Verification Status', 'OCR Confidence'];
    
    const rows = questions.map(q => [
      `"${(q.id || '').replace(/"/g, '""')}"`,
      `"${(q.question_number || '').replace(/"/g, '""')}"`,
      `"${(q.verified_question_text || q.question_text || '').replace(/"/g, '""')}"`,
      `"${(q.subject || '').replace(/"/g, '""')}"`,
      `"${(q.exam_year || '').replace(/"/g, '""')}"`,
      `"${(q.section || '').replace(/"/g, '""')}"`,
      `"${(q.marks || 1)}"`,
      `"${(q.chapter || '').replace(/"/g, '""')}"`,
      `"${(q.topic || '').replace(/"/g, '""')}"`,
      `"${(q.verification_status || 'Needs Verification')}"`,
      `"${(q.ocr_confidence || 0.85)}"`
    ]);

    const csvContent = '\uFEFF' + [headers.join(','), ...rows.map(r => r.join(','))].join('\r\n');
    const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.setAttribute('href', url);
    link.setAttribute('download', filename);
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
  },

  // Export DOCX / Word compatible document
  exportDOCX(data, filename = 'exam_pattern_report.doc') {
    const questions = data.questions || [];
    const repeats = data.repeats || [];
    const historical = data.historical || [];
    const topics = data.topics || [];

    let html = `<!DOCTYPE html>
<html xmlns:o='urn:schemas-microsoft-com:office:office' xmlns:w='urn:schemas-microsoft-com:office:word' xmlns='http://www.w3.org/TR/REC-html40'>
<head>
<meta charset='utf-8'>
<title>Exam Pattern Analyzer Report</title>
<style>
body { font-family: 'SolaimanLipi', 'Kalpurush', 'Times New Roman', sans-serif; line-height: 1.6; }
h1 { color: #1e3a8a; border-bottom: 2px solid #1e3a8a; padding-bottom: 6px; }
h2 { color: #2563eb; margin-top: 24px; border-bottom: 1px solid #cbd5e1; }
table { width: 100%; border-collapse: collapse; margin-top: 12px; margin-bottom: 20px; }
th, td { border: 1px solid #94a3b8; padding: 8px 10px; font-size: 11pt; text-align: left; }
th { background-color: #f1f5f9; font-weight: bold; }
.badge { font-weight: bold; color: #2563eb; }
.footer { font-size: 9pt; color: #64748b; margin-top: 30px; text-align: center; }
</style>
</head>
<body>
<h1>Exam Pattern Analyzer (EPA) - Official Report</h1>
<p><strong>Brand:</strong> Developed by Mehedi364 | <strong>Tagline:</strong> Analyze. Understand. Practice.</p>
<p><strong>Generated on:</strong> ${new Date().toLocaleDateString('bn-BD')} (${new Date().toLocaleString()})</p>
<p><strong>Total Questions:</strong> ${questions.length} | <strong>Repeated Groups:</strong> ${repeats.length} | <strong>Historical Dates:</strong> ${historical.length}</p>

<h2>1. Repeated Questions Analysis</h2>
<table>
<thead><tr><th>#</th><th>Question Text</th><th>First Appeared</th><th>Also Appeared</th><th>Total Occurrences</th></tr></thead>
<tbody>
${repeats.slice(0, 20).map((r, i) => `
<tr>
  <td>${i + 1}</td>
  <td>${r.sampleQuestion.verified_question_text || r.sampleQuestion.question_text}</td>
  <td>${r.firstAppeared}</td>
  <td>${r.alsoAppeared.join(', ') || 'N/A'}</td>
  <td><strong>${r.totalOccurrences}</strong></td>
</tr>`).join('')}
</tbody>
</table>

<h2>2. Topic Frequency Analysis</h2>
<table>
<thead><tr><th>Topic</th><th>Question Count</th><th>Percentage</th><th>Years Covered</th></tr></thead>
<tbody>
${topics.slice(0, 15).map(t => `
<tr>
  <td>${t.topic}</td>
  <td>${t.count}</td>
  <td>${t.percentage}%</td>
  <td>${t.years.join(', ')}</td>
</tr>`).join('')}
</tbody>
</table>

<h2>3. Important Historical Dates Extracted</h2>
<table>
<thead><tr><th>Year/Date</th><th>Event Hint</th><th>Source Question</th><th>Exam Year</th></tr></thead>
<tbody>
${historical.slice(0, 25).map(h => `
<tr>
  <td><strong>${h.historical_date} (${h.bengali_date})</strong></td>
  <td>${h.event_hint}</td>
  <td>${h.question_text}</td>
  <td>${h.exam_year || 'N/A'}</td>
</tr>`).join('')}
</tbody>
</table>

<div class='footer'>
  <p>Generated by Exam Pattern Analyzer (EPA) &bull; Developed by Mehedi364 &bull; All statistical data verified from database</p>
</div>
</body>
</html>`;

    const blob = new Blob(['\uFEFF' + html], { type: 'application/msword;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.setAttribute('href', url);
    link.setAttribute('download', filename);
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    URL.revokeObjectURL(url);
  },

  // Print/PDF View
  openPrintableReport(data) {
    const w = window.open('', '_blank');
    if (!w) {
      alert('Please allow popups to generate printable report.');
      return;
    }

    const questions = data.questions || [];
    const repeats = data.repeats || [];
    const historical = data.historical || [];
    const topics = data.topics || [];

    w.document.write(`
      <!DOCTYPE html>
      <html>
      <head>
        <title>Exam Pattern Analyzer - Analysis Report</title>
        <style>
          body { font-family: 'SolaimanLipi', -apple-system, sans-serif; padding: 30px; color: #1e293b; max-width: 900px; margin: 0 auto; }
          .header { text-align: center; border-bottom: 2px solid #2563eb; padding-bottom: 16px; margin-bottom: 24px; }
          h1 { color: #1e3a8a; margin: 0 0 6px 0; }
          .meta { font-size: 0.9rem; color: #64748b; }
          table { width: 100%; border-collapse: collapse; margin: 16px 0 28px 0; }
          th, td { border: 1px solid #cbd5e1; padding: 8px 12px; text-align: left; font-size: 0.92rem; }
          th { background: #f8fafc; font-weight: 700; color: #1e293b; }
          .badge { padding: 3px 8px; border-radius: 12px; font-weight: 600; font-size: 0.8rem; background: #e0e7ff; color: #3730a3; }
          @media print {
            .no-print { display: none; }
            body { padding: 0; }
          }
        </style>
      </head>
      <body>
        <div class="no-print" style="margin-bottom: 20px; text-align: right;">
          <button onclick="window.print()" style="padding: 10px 20px; background: #2563eb; color: #fff; border: none; border-radius: 6px; cursor: pointer; font-weight: bold;">Print / Save as PDF</button>
        </div>
        <div class="header">
          <h1>Exam Pattern Analyzer (EPA)</h1>
          <p class="meta">Analyze. Understand. Practice. &bull; Developed by Mehedi364</p>
          <p class="meta">Comprehensive Pattern & Question Frequency Report &bull; ${new Date().toLocaleDateString()}</p>
        </div>
        
        <h3>1. Summary Overview</h3>
        <table>
          <tr><td>Total Verified Questions</td><td><strong>${questions.length}</strong></td></tr>
          <tr><td>Repeated Question Groups</td><td><strong>${repeats.length}</strong></td></tr>
          <tr><td>Distinct Topics Covered</td><td><strong>${topics.length}</strong></td></tr>
          <tr><td>Historical Dates Extracted</td><td><strong>${historical.length}</strong></td></tr>
        </table>

        <h3>2. Frequently Repeated Questions</h3>
        <table>
          <thead><tr><th>#</th><th>Question</th><th>Years</th><th>Count</th></tr></thead>
          <tbody>
            ${repeats.slice(0, 15).map((r, i) => `
              <tr>
                <td>${i+1}</td>
                <td>${r.sampleQuestion.verified_question_text || r.sampleQuestion.question_text}</td>
                <td>${r.allYears.join(', ')}</td>
                <td><span class="badge">${r.totalOccurrences} times</span></td>
              </tr>
            `).join('')}
          </tbody>
        </table>

        <h3>3. Topic Frequency Distribution</h3>
        <table>
          <thead><tr><th>Topic</th><th>Questions</th><th>Percentage</th><th>Years</th></tr></thead>
          <tbody>
            ${topics.slice(0, 15).map(t => `
              <tr>
                <td>${t.topic}</td>
                <td><strong>${t.count}</strong></td>
                <td>${t.percentage}%</td>
                <td>${t.years.join(', ')}</td>
              </tr>
            `).join('')}
          </tbody>
        </table>

        <h3>4. Extracted Historical Dates (Important Years)</h3>
        <table>
          <thead><tr><th>Historical Date</th><th>Event Context</th><th>Exam Year</th></tr></thead>
          <tbody>
            ${historical.slice(0, 20).map(h => `
              <tr>
                <td><strong>${h.historical_date} CE (${h.bengali_date})</strong></td>
                <td>${h.event_hint}</td>
                <td>${h.exam_year || 'N/A'}</td>
              </tr>
            `).join('')}
          </tbody>
        </table>
      </body>
      </html>
    `);
    w.document.close();
  }
};
